using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemInfomation : MonoBehaviour
{
    public enum ItemColor { Red, Green, Blue, Orange, Brown };
    public ItemColor Color;

    public bool isFitting = false;
}
